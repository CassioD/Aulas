const numeros = ['um', 'dois', 'três'];

for (let contador = 0; contador < numeros.length; contador++){
    const elemento = numeros[contador];
    console.log(`Elemento #${contador}: ${elemento}`);
}